node_modules
.next
out
.env
.env.local
.env*.local
*.log
.DS_Store
.vercel

# Local database — your data, never committed
data/db.json

# TypeScript incremental build cache — machine-local, never committed
tsconfig.tsbuildinfo
