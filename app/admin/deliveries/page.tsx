import { AdminDeliveries } from "@/components/admin/admin-deliveries";

export const metadata = { title: "Alert deliveries — Admin" };

export default function Page() {
  return <AdminDeliveries />;
}
