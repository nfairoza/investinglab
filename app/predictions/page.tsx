import { PageShell } from "@/components/page-shell";

export default function Page() {
  return (
    <PageShell
      title="Predictions"
      phase="Phase 8"
      first="These are market-implied odds, not predictions — watch which way they're moving over time."
    />
  );
}
